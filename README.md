# fashion-item-classification-using-convolutional-neural-network
A deep learning project that uses a Convolutional Neural Network (CNN) to classify different types of clothing images from the Fashion MNIST dataset. The model performs image preprocessing, training, evaluation, and visualizes performance using accuracy and loss graphs.
